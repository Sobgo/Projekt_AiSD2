#include <string>
#include <unordered_map>
#include <vector>

using namespace std;

// NOLINTNEXTLINE(misc-unused-parameters)
unordered_map<string, vector<int>> match(const vector<string> &patterns, const string &text) {
	return {};
}
